#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int price, h;
    cin >> price >> h;

    for (int i = 1; i <= 10; ++i) {
        if (i * price % 10 == 0 || i * price % 10 == h) {
            cout << i;
            return 0;
        }
    }

    return 0;
}
