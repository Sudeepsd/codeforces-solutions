#include <bits/stdc++.h>
using namespace std;

void solve() {
    int a; cin >> a;

    cout << 1 << " " << a - 1;

    cout << "\n";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int T; cin >> T;
    while (T--) {
        solve();
    }

    return 0;
}
