#include <bits/stdc++.h>
using namespace std;

void solve() {
    cout << "Red panda";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int T = 1;
    // cin >> T;
    while (T--) {
        solve();
    }

    return 0;
}
